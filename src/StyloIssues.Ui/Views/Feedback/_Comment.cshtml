@model IssueComment
<div class="comment">
    <strong>@Model.Author</strong>
    @if (Model.FromApp)
    {
        <span>[bot]</span>
    }
    <span>on @Model.CreatedAt.ToString("yyyy-MM-dd HH:mm")</span>
    <p>@Model.Body</p>
</div>
